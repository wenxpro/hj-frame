# 提前禁用 Nacos 客户端自动注入的 logback 配置（读系统属性，需在 Nacos 日志初始化前设置）：
# 用 spring.config.import 加载 Nacos 配置时，NacosConfigDataLoader 会在环境准备阶段初始化
# Nacos 客户端并应用其 logback 配置；随后 NacosLoggingAppRunListener 再次应用同一配置，
# 把 nacos/config.log 等 appender 重复加载，导致 "Logback configuration error" 启动失败。
# 自定义 listener 在构造阶段（早于一切 environmentPrepared）设置系统属性将其关闭。
# 放 v3-log 使三服务（auth/system/gateway，均依赖 v3-log-starter）统一生效。
org.springframework.boot.SpringApplicationRunListener=\
com.wenx.v3log.config.NacosLoggingDisableRunListener
